generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// Tambahkan model Anda di sini
model User {
  id        String   @id @default(cuid())
  name      String
  email     String   @unique
  password  String
  role      String   @default("USER")
  createdAt DateTime @default(now())
}

model ZakatTransaction {
  id            String   @id @default(cuid())
  name          String   @default("Hamba Allah")
  message       String?  @db.Text                
  zakatType     String   
  amount        Float
  paymentMethod String
  status        String   @default("PENDING") 
  createdAt     DateTime @default(now())
}

model SppTransaction {
  id            String   @id @default(cuid())
  studentName   String
  message       String
  sppType       String
  paymentMonth  String
  amount        Int
  status        String   @default("PENDING")
  createdAt     DateTime @default(now())
}

// Tambahkan di baris paling bawah
model Withdrawal {
  id          String   @id @default(cuid())
  amount      Float    // Nominal yang ditarik
  source      String   // "ZAKAT" atau "SPP"
  note        String?  // Keterangan penarikan
  withdrawnBy String   // Nama admin yang menarik
  createdAt   DateTime @default(now())
}